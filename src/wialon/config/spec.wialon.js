import { flags } from './flags.wialon.js';

export const avl_unit = [
    { type: "type", data: "avl_unit", flags: flags, mode: 0 }
];

export const avl_unit_group = [
    { type: 'type', data: 'avl_unit_group', flags: flags, mode: 0 }
];

export const avl_resource = [
    { type: "type", data: "avl_resource", flags: flags, mode: 0 }
];

export const all_avl = [
    { type: "type", data: "avl_unit", flags: flags, mode: 0 },
    { type: 'type', data: 'avl_unit_group', flags: flags, mode: 0 },
    { type: "type", data: "avl_resource", flags: flags, mode: 0 }
];